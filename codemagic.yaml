workflows:
  main-workflow:
    name: Sanad APK Build
    instance_type: mac_mini_m1
    scripts:
      - npm install
      - cd android && ./gradlew assembleDebug
    artifacts:
      - android/app/outputs/apk/**/*.apk
