# ── keystore.properties ──────────────────────────────────────────────────────
# Copy this file to: artifacts/digital-city/android/keystore.properties
# Fill in your values after running the keytool command below.
# ⚠️  NEVER commit keystore.properties or sanad-release.jks to git!
# ─────────────────────────────────────────────────────────────────────────────

storeFile=../sanad-release.jks
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=sanad
keyPassword=YOUR_KEY_PASSWORD
